{
  "name": "csv-to-lua-watch",
  "version": "1.0.0",
  "description": "test to watch and respond to file changes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pkg index.js --targets win --output dist/csv-to-lua-watch.exe"
  },
  "author": "Dan Fessler",
  "license": "ISC",
  "dependencies": {
    "lua-json": "^1.0.0",
    "papaparse": "^5.2.0",
    "prompt": "^1.0.0"
  },
  "devDependencies": {
    "pkg": "^4.4.9"
  }
}
